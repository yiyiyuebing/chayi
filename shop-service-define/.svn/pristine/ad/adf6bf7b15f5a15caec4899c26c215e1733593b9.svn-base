package pub.makers.shop.baseGood.service;

import java.util.List;

import pub.makers.shop.baseOrder.enums.OrderBizType;
import pub.makers.shop.tradeGoods.entity.TradeGiftRule;

/**
 * 赠品规则服务
 * @author apple
 *
 */
public interface GiftBizService {

	
	/**
	 * 查询SKU匹配的赠品规则
	 * @param orderBizType
	 * @param goodSkuId
	 * @return
	 */
	List<TradeGiftRule> queryGiftRules(OrderBizType orderBizType, String goodSkuId);
}
