package pub.makers.shop.tongji.service;

import pub.makers.shop.baseOrder.enums.OrderBizType;
import pub.makers.shop.tongji.pojo.ShopAccessParam;

/**
 * 店铺访问记录服务
 * @author apple
 *
 */
public interface ShopAccessRecordBizService {

	/**
	 * 进入页面
	 * @param param
	 */
	void enter(ShopAccessParam param);
	
	/**
	 * 离开页面
	 * @param requestId
	 */
	void leave(OrderBizType orderBizType, String requestId);
}
